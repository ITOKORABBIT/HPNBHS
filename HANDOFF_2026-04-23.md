# Handoff 2026-04-23

這份文件記錄 2026-04-23 針對表單、詳情頁與 Make webhook 做過的修正，供後續 Claude / Codex / 維護者快速接手。

## 已完成變更

### 1. Make webhook 防誤觸發

- 已在 `MAKE scenarios/` 產出：
  - `HPNBHS - New Report Push 0423.json`
  - `HPNBHS - New Store Push 0423.json`
  - `MAKE webhook hardening 0423.md`
- 設計原則：
  - 只允許合法請求進入 LINE 推播
  - 條件包含：
    - POST method
    - query token 正確
    - 必填欄位完整且非空
- Make 實際 webhook method 欄位名稱不是 `method`，而是 `__IMTMETHOD__`
- `apps-script.gs` 已改為帶 token：
  - `REPORT_WEBHOOK_URL ... ?token=ITOKO`
  - `STORE_NOTIFY_WEBHOOK_URL ... ?token=ITOKO`

### 2. 表單類別恢復為短版

- `report.html`
  - 類別已恢復為：
    - `生活`
    - `校園`
    - `交通`
    - `環境`
    - `治安`
    - `修繕`
    - `其他`
- `store.html`
  - 類別已恢復為：
    - `食`
    - `衣`
    - `住`
    - `行`
    - `育`
    - `樂`
    - `其他`

### 3. 壞字清理

- 已確認以下檔案目前沒有壞字殘留：
  - `report.html`
  - `store.html`
  - `detail.html`
  - `storedetail.html`

### 4. 照片按鈕行為

- `report.html`
  - 公開表單照片區已恢復舊版樣式的白色膠囊 `移除` 按鈕
- `store.html`
  - 公開表單照片區已恢復舊版樣式的白色膠囊 `移除` 按鈕
- `detail.html`
  - 詳情頁展示照片已加入 `下載`
  - 回覆 modal 的照片槽有：
    - `移除`
    - `下載`
- `storedetail.html`
  - 詳情頁展示照片已加入 `下載`
  - 審核 modal 的商店照片槽有：
    - `移除`
    - `下載`

## 後續維護注意

### A. 不要再用有壞字的版本覆蓋以下檔案

- `report.html`
- `store.html`
- `detail.html`
- `storedetail.html`

如果要從舊 commit 回復內容，先確認該版本不是壞字版本。

### B. 已知乾淨版本參考

- `report.html` / `store.html` / `storedetail.html`
  - `ea6083e8` 是乾淨基底之一
- `detail.html`
  - `e1f8f199` 是乾淨版本之一

### C. 若 Make 又不推播，先檢查

1. Filter 是否使用 `__IMTMETHOD__`
2. token 是否仍為 `ITOKO`
3. Apps Script 是否已部署到最新版本

## 本次相關 commit

- `edb35d5` Restore photo remove buttons on public forms
- `a2c0ca7` Restore clean detail pages and add photo downloads
- `7f1e4e6` Restore form text and add detail photo actions
- `eacf7d9` Restore store detail photo editor and downloads
